# Changelog

## [Unreleased] - 2025-11-27

### 🚀 Major Changes - Optimized Data Update Strategy

#### Переработана система запросов к API
Реализована новая оптимизированная стратегия обновления данных, которая значительно снижает нагрузку на API при сохранении актуальности информации.

### ✨ Added

#### 1. Раздельные интервалы обновления
- **Данные за сегодня:** Обновляются каждые 10 минут (настраивается)
- **Данные за месяц:** Обновляются один раз в день в 00:01
- **Данные за все время:** Обновляются один раз в день в 00:01

#### 2. Интеллектуальный расчет общего количества заказов
- База данных (все время без сегодня) загружается 1 раз в день
- Текущее значение рассчитывается локально: `база + сегодня`
- Автоматическое обновление при каждом обновлении данных за сегодня
- Никаких дополнительных запросов к серверу

#### 3. Планировщик ежедневных обновлений
- Автоматическое обновление в заданное время (по умолчанию 00:01)
- Настраиваемое время в конфигурации
- Автоматическое планирование следующего обновления

### 🔧 Changed

#### Конфигурация (`src/config.js`)
```javascript
// Было:
apiRefreshInterval: 600000  // Для всех данных

// Стало:
todayRefreshInterval: 600000     // Только для данных за сегодня
dailyUpdateTime: '00:01'         // Время ежедневного обновления
```

#### DataStore (`src/services/dataStore.js`)
- Разделены функции загрузки данных: `fetchTodayData()`, `fetchMonthlyData()`, `fetchTotalData()`
- Добавлен планировщик `scheduleDailyUpdate()` для обновлений в 00:01
- Добавлено хранение базового значения `totalExcludingToday`
- Автоматический пересчет общего количества при обновлении данных за сегодня

#### Orders API (`src/services/ordersApi.js`)
- Добавлены раздельные мок-функции для каждого типа данных
- `getTodayOrders()` теперь с автоматическим fallback на мок-данные
- `getMonthlyOrders()` с автоматическим fallback на мок-данные
- `getTotalOrders()` с параметром `exclude_today=true` по умолчанию
- Добавлены новые мок-функции: `generateMockTodayData()`, `generateMockMonthlyData()`, `generateMockTotalData()`

#### DashboardCard (`src/components/DashboardCard.vue`)
- Обновлена функция `updateFromCache()` для работы с новой структурой данных
- Добавлены проверки наличия данных перед обновлением

### 📚 Documentation

#### Новые документы
- **`DATA_UPDATE_LOGIC.md`** - Подробное описание логики обновления данных
  - Стратегия обновления для каждого типа данных
  - Визуализация расписания обновлений
  - Примеры конфигурации
  - Диаграммы последовательности
  - Руководство по тестированию

#### Обновленные документы
- **`API_SPECIFICATION.md`** - Добавлен раздел "Логика обновления данных"
  - Описание параметра `exclude_today` для `/total` endpoint
  - Рекомендации по кэшированию на сервере
  - Объяснение преимуществ новой схемы

### 📊 Performance Improvements

#### Снижение нагрузки на API
- **До:** ~4,320 запросов в день для 10 клиентов
- **После:** ~1,460 запросов в день для 10 клиентов
- **Экономия:** 66% запросов

#### Оптимизация трафика
- Исторические данные (monthly, total) обновляются 1 раз в сутки
- Частые обновления только для малого объема данных (today)
- Локальный пересчет общего количества без запросов к серверу

### 🔍 Technical Details

#### Структура данных клиента
```javascript
{
  client_id: "yakitoriya",
  client_name: "Якитория",
  color: "#dc2626",
  today: { ... },                    // Обновляется каждые 10 мин
  monthly: { ... },                  // Обновляется в 00:01
  total: { orders_count: ... },      // Рассчитывается локально
  totalExcludingToday: ...           // База с сервера (обновляется в 00:01)
}
```

#### Формула расчета
```
total.orders_count = totalExcludingToday + today.orders_count
```

### 🐛 Bug Fixes
- Исправлена обработка отсутствующих данных в DashboardCard
- Добавлены проверки наличия данных перед обновлением UI

### 🎯 Breaking Changes
- ⚠️ Изменена структура данных в `clientsData`
- ⚠️ `apiRefreshInterval` переименован в `todayRefreshInterval`
- ⚠️ Добавлен новый параметр конфигурации `dailyUpdateTime`

### 🔄 Migration Guide

Если вы обновляете существующий проект:

1. Обновите `src/config.js`:
   ```javascript
   // Переименуйте
   apiRefreshInterval → todayRefreshInterval
   
   // Добавьте
   dailyUpdateTime: '00:01'
   ```

2. Убедитесь, что ваш backend API поддерживает параметр `exclude_today` для endpoint `/total`

3. Проверьте логи в консоли браузера для корректной работы планировщика

### 📝 Notes

- Все изменения обратно совместимы с мок-данными
- При недоступности API автоматически используются мок-данные
- Подробное логирование всех операций в консоль браузера

### 🎨 UI Changes
- Увеличен размер шрифта названий клиентов (1.1rem → 1.5rem)
- Увеличена жирность шрифта названий клиентов (bold → 900)

---

## Previous Changes

### [1.0.0] - 2025-11-26
- Initial release with dashboard, carousel, and API integration
- 6-card layout with 2 rows × 3 columns
- Dynamic client loading from API
- Charts with vertical and horizontal grid lines
- Weather widget and birthdays integration
