# DGTV Dashboard - Руководство по интеграции

## 📋 Обзор

Dashboard отображает статистику заказов для трёх клиентов:
- **Якитория** (красный)
- **ServiceGuru** (зелёный)  
- **МКК** (оранжевый)

Каждая карточка показывает:
1. **СЕГОДНЯ** - количество заказов с 00:00 до текущего момента
2. **ЗА МЕСЯЦ** - график заказов за последние 30 дней (без текущего дня)
3. **ЗА ВСЕ ВРЕМЯ** - общее количество заказов за все время

---

## 🔄 Обновление данных

### Автоматическое обновление
- Каждая карточка обновляется **каждые 10 минут**
- Первая загрузка происходит при монтировании компонента
- Интервал автоматически очищается при размонтировании

### Ручное обновление
Для ручного обновления данных просто обновите страницу браузера.

---

## 🔌 Интеграция с внешним API

### 1. Конфигурация

Отредактируйте файл `.env` или `.env.local`:

```bash
# URL вашего API
VITE_API_URL=https://api.your-domain.com/v1

# Bearer токен для аутентификации (опционально)
VITE_API_TOKEN=your_secret_token_here
```

### 2. API Endpoints

Приложение использует комбинированный endpoint для оптимизации:

```http
GET /clients/{client_id}/orders/dashboard
```

**Параметры:**
- `client_id`: `yakitoriya`, `serviceguru`, или `mkk`

**Пример ответа:**
```json
{
  "client_id": "yakitoriya",
  "client_name": "Якитория",
  "color": "#dc2626",
  "today": {
    "date": "2025-11-26",
    "orders_count": 1920,
    "period": {
      "from": "2025-11-26T00:00:00Z",
      "to": "2025-11-26T14:30:00Z"
    }
  },
  "monthly": {
    "period": {
      "from": "2025-10-27",
      "to": "2025-11-25"
    },
    "daily_orders": [1200, 1500, 1300, ...], // 30 значений
    "total": 45230,
    "average": 1507.67
  },
  "total": {
    "orders_count": 4993238,
    "since": "2020-01-01"
  },
  "updated_at": "2025-11-26T14:30:00Z",
  "next_update": "2025-11-26T14:40:00Z"
}
```

### 3. Альтернативные endpoints (детальные)

Если вам нужны отдельные endpoints:

#### Заказы за сегодня
```http
GET /clients/{client_id}/orders/today
```

#### Заказы за месяц
```http
GET /clients/{client_id}/orders/monthly?days=30&exclude_today=true
```

#### Общая статистика
```http
GET /clients/{client_id}/orders/total
```

Подробная спецификация API находится в файле `API_SPECIFICATION.md`.

---

## 🧪 Режим разработки (мок-данные)

По умолчанию приложение использует **мок-данные** для разработки:

### Автоматический fallback
Если API недоступен, приложение автоматически переключается на генерацию тестовых данных:

```javascript
import { getClientDashboardSafe } from '../services/ordersApi'

// Автоматически использует мок-данные при ошибке API
const data = await getClientDashboardSafe(clientId, clientName, color)
```

### Генерация мок-данных
Тестовые данные генерируются случайным образом:
- Заказы за сегодня: **800-2500**
- Заказы по дням (30 дней): **900-2400** каждый
- Всего заказов: **2M-7M**

### Логирование
Проверьте консоль браузера для отладки:
```
✅ Данные для Якитория обновлены: { сегодня: 1920, месяц: '30 дней', всего: 4993238 }
🔄 Автообновление для Якитория запущено (каждые 10 минут)
⏹️ Автообновление для Якитория остановлено
```

---

## 🎨 Добавление новых клиентов

### 1. Обновите Home.vue

Добавьте новую карточку:

```vue
<DashboardCard
  client-id="new-client"
  client-name="Название клиента"
  color="#3b82f6"
/>
```

### 2. Настройте API

Убедитесь, что ваш backend поддерживает:
```http
GET /clients/new-client/orders/dashboard
```

### 3. Настройте grid (опционально)

Для 4 карточек измените layout в Home.vue:
```css
.cards-grid {
  grid-template-columns: repeat(2, 1fr);
  grid-template-rows: repeat(2, 1fr);
}
```

---

## 🚀 Production deployment

### 1. Создайте production .env

```bash
# .env.production
VITE_API_URL=https://api.your-domain.com/v1
VITE_API_TOKEN=your_production_token
```

### 2. Build проекта

```bash
npm run build
```

### 3. Preview сборки

```bash
npm run preview
```

### 4. Deploy

Загрузите содержимое папки `dist/` на ваш сервер.

---

## 🔐 Безопасность

### Рекомендации:
1. **Не коммитьте** `.env.local` в репозиторий
2. Используйте **HTTPS** для production API
3. Ограничьте **CORS** на backend только для TV-бокса
4. Используйте **rate limiting** на API (100 req/min)
5. Ротируйте **токены** регулярно

### Пример CORS конфигурации (Node.js Express):
```javascript
const cors = require('cors');

app.use(cors({
  origin: ['http://10.10.10.90:5173', 'https://tv-dashboard.your-domain.com'],
  credentials: true
}));
```

---

## 📊 Мониторинг

### Console logs

Приложение логирует все операции:
```javascript
✅ Данные для ServiceGuru обновлены
❌ Ошибка загрузки данных для МКК: HTTP 500
⚠️ Не удалось загрузить данные для yakitoriya, используем мок-данные
```

### Error handling

При ошибках API приложение:
1. Логирует ошибку в консоль
2. Автоматически переключается на мок-данные
3. Продолжает работу без прерывания

---

## 🛠 Troubleshooting

### Проблема: Карточки не обновляются

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что API URL корректный в `.env`
3. Проверьте доступность API: `curl https://api.your-domain.com/v1/clients/yakitoriya/orders/dashboard`
4. Проверьте токен аутентификации

### Проблема: Показываются одинаковые данные

**Решение:**
- Это нормально для мок-данных (они генерируются случайно при каждом запуске)
- Для реальных данных убедитесь, что API возвращает разные значения для разных клиентов

### Проблема: График не отображается

**Решение:**
1. Проверьте, что API возвращает массив `daily_orders` с 30 значениями
2. Откройте Developer Tools → Console и проверьте данные
3. Убедитесь, что все значения в массиве являются числами

### Проблема: Ошибка CORS

**Решение:**
1. Настройте CORS на backend для разрешения запросов с TV-бокса
2. Добавьте IP адрес TV-бокса в whitelist
3. Убедитесь, что заголовок `Authorization` разрешён в CORS

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте логи на backend
3. Убедитесь, что все endpoints API работают корректно
4. Проверьте формат ответов API согласно спецификации

---

## 📝 Структура проекта

```
dgtv/
├── src/
│   ├── components/
│   │   └── DashboardCard.vue    # Карточка клиента с автообновлением
│   ├── services/
│   │   └── ordersApi.js         # API сервис с мок-данными
│   └── views/
│       └── Home.vue             # Главная страница с 3 карточками
├── .env                         # Конфигурация API (шаблон)
├── .env.local                   # Локальная конфигурация
├── API_SPECIFICATION.md         # Полная спецификация API
└── INTEGRATION_GUIDE.md         # Этот файл
```

---

## ✅ Checklist интеграции

- [ ] Создан API endpoint `/clients/{client_id}/orders/dashboard`
- [ ] API возвращает корректный JSON формат
- [ ] Настроены CORS заголовки на backend
- [ ] Создан и настроен `.env.local` файл
- [ ] API URL и токен заполнены в `.env`
- [ ] Проверена работа с реальными данными
- [ ] Настроен production environment
- [ ] Проверена работа автообновления (10 минут)
- [ ] Deploy на сервер выполнен
- [ ] TV-бокс подключён к дашборду
